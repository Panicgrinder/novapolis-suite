# Projekt-Einstellungen
PROJECT_NAME=CVN Agent
PROJECT_DESCRIPTION=Local RPG Agent Backend
PROJECT_VERSION=0.1.0

# CORS-Einstellungen
# Kommagetrennte Liste von erlaubten Ursprüngen
BACKEND_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:5173
# Optional: Regulärer Ausdruck für erlaubte Ursprünge (überschreibt BACKEND_CORS_ORIGINS wenn gesetzt)
# ALLOW_ORIGIN_REGEX=^https?://(localhost|127\.0\.0\.1)(:\d+)?$

# API-Einstellungen
AGENT_PORT=8765
OLLAMA_HOST=http://localhost:11434
MODEL_NAME=llama3.1:8b

# OpenAI (nur lokal im Workspace .env verwenden, nicht committen)
OPENAI_API_KEY=

# Datenbank-Einstellungen
DB_URL=sqlite:///./data/state.sqlite3

# Weitere Einstellungen können hier hinzugefügt werden