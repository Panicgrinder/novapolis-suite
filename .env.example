# Beispiel-Umgebungsvariablen (Root) – kopieren nach .env und Werte setzen
# Diese Datei niemals für echte Secrets verwenden.
# DO NOT COMMIT – rotate if exposed.
# Erstellt: 2025-11-07 (Migration aus novapolis_agent/.env.example)

# Basis
DEBUG=true
PROJECT_NAME=CVN Agent
PROJECT_DESCRIPTION=Local RPG Agent Backend
PROJECT_VERSION=0.1.0

# API / Modell
AGENT_PORT=8765
OLLAMA_HOST=http://localhost:11434
MODEL_NAME=llama3.1:8b

# Datenbank
DB_URL=sqlite:///./data/state.sqlite3

# CORS (entweder Liste oder Regex nutzen – Regex überschreibt Liste, wenn gesetzt)
BACKEND_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:5173
#ALLOW_ORIGIN_REGEX=^https?://(localhost|127\.0\.0\.1)(:\d+)?$

# OpenAI (leer lassen wenn nicht genutzt)
OPENAI_API_KEY=

# Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS_PER_MINUTE=60
RATE_LIMIT_BURST=20
REQUEST_MAX_TOKENS=10024

# Hinweise:
# 1) .env ist in .gitignore eingetragen – sicherstellen, dass diese Datei nicht versehentlich umbenannt wird.
# 2) Bei CI-Verwendung Secrets über Workflow-Umgebungsvariablen injizieren.
# 3) Zusätzliche Variablen für Features (RAG, Memory) in Agent-Code definieren, hier nur Kern.

# Lokale AI (S2)
RAG_ENABLED=true
RAG_INDEX_PATH=novapolis_agent/eval/results/rag/s2-core-index-20260218.json
RAG_TOP_K=3
SHADOW_MODE_LOGGING_ENABLED=true
SHADOW_MODE_LOG_PATH=.tmp/results/logs/shadow_mode.jsonl
# Alias-Flags fuer TODO/Go-NoGo (werden intern auf bestehende Settings gemappt)
RAG_ON=true
SHADOW_ON=true
CANARY_PCT=0
